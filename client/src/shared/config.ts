// Runtime endpoints for the backend. Centralised so infrastructure clients
// receive their URLs via injection instead of hard-coding them inline.
export const API_BASE_URL = "http://localhost:3001";
export const WS_URL = "ws://localhost:3001/ws";
