{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node --env-file=.env dist/app.js",
    "dev": "npm run build && concurrently -k -p \"[{name}]\" -n \"TypeScript,App\" -c \"yellow.bold,cyan.bold\" \"tsc -w\" \"node --env-file=.env --watch dist/app.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/multipart": "^10.0.0",
    "@fastify/websocket": "^11.2.0",
    "@kubernetes/client-node": "^1.4.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "concurrently": "^9.0.0",
    "typescript": "~5.9.0"
  }
}
